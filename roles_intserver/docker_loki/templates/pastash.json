

input {
  udp {
    host => 0.0.0.0
    port => 5140
    type => syslog
  }
}

filter {
  if [type] == syslog {
    regex {
      builtin_regex => syslog
    }
    syslog_pri {}
  }
}

output {
  loki {
    host => "app"
    port => 3100
    path => "/api/prom/push"
  }
}

