
// Netzwerk Geräte 

network:pingdevice:handy_privat [ hostname="{{ networks.local.fixed_ip.handy_privat }}", retry=1, timeout=5000, refreshInterval=10000, uses_arp_ping="yes" ]
network:pingdevice:handy_arbeit [ hostname="{{ networks.local.fixed_ip.handy_arbeit }}", retry=1, timeout=5000, refreshInterval=10000, uses_arp_ping="yes" ]
