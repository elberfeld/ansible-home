
// Switche für 433MHz Funksteckdosen 
// Die Ansteuerung erfolgt über das exec-binding

{% for device in rfswitches %}

String {{ device.id }} "{{ device.name }}" ({{ device.groups }}) [ "Switchable" ] { channel="exec:command:{{ device.id }}:input", autoupdate="true" }

{% endfor %}
