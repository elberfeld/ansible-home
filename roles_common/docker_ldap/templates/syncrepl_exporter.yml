---

    ldap:
      uri: 'ldap://openldap'
      basedn: '{{ ldap_global.search_base }}'
      starttls: false
      bind: true
      binddn: '{{ ldap_global.readonly_dn }}'
      bindpass: '{{ ldap_readonly_pass }}'
